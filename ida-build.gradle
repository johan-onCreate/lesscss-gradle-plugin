buildscript {
    repositories {
        maven { url "http://repo.idainfront.se/content/groups/internal" }
    }

    dependencies {
        classpath "com.iipax.gradle.plugins:ida-maven-publish:5.6"
    }
}

apply plugin: "com.iipax.maven-publish"
apply plugin: 'eclipse'

apply from: "build.gradle"